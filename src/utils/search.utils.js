// export const handleEnterKeyPress = (event, searchProps) => {
//   if (event.key === 'Enter') {
//     if (searchProps.name === "Email") onSearchEmailHandler(searchProps.value);
//     if (searchProps.name === "UPC") onSearchUPCHandler(searchProps.value);
//   }
// }